---
name: jianying-autoedit-agent
description: 在 Windows 或 macOS 的剪映专业版中完成基于事实资料与素材语义的自动剪辑。适用于先整理事实、完整扫描视频、识别真实场景与动作、按画面顺序和有效时长重写口播、逐句匹配源区间、建立节奏时间线，再生成剪映配音与智能字幕、使用“智能包装”美化字幕、从剪映音乐库选择不重复的纯音乐并审片导出；尤其适合房产看房、空间导览、产品讲解和有明确事实资料的视频。
---

# 剪映语义自动剪辑

把 DaVinci 自动剪辑工作流的“素材审查、语义分段、蓝图、写入后审计”用于 Windows 或 macOS 版剪映。剪映只是执行端；不能因为素材已经在时间线里，就跳过内容识别并把整段视频统一变速来迁就旁白。

## 必须达到的结果

```text
事实资料整理 → 素材完整扫描 → 密集抽帧与场景识别 → 源区间场景地图
→ 按真实顺序和画面时长重写口播 → 逐句口播/画面匹配
→ 多切点节奏蓝图 → 剪映搭建 → 配音 → 智能字幕 → 智能包装
→ 剪映曲库纯音乐
→ 完整审片与文件验证 → 成片＋可编辑草稿
```

原素材不可修改。已有草稿先保存并备份；重大修改使用新草稿版本。

房产视频未另行指定时，沿用源素材的竖屏 `9:16` 画幅，使用剪映年轻男声“小帅”和自然、清楚的房产介绍语气，并从剪映音乐库选择不过度抢声的纯音乐。若“小帅”、智能包装或目标音乐因剪映版本、账号或地区不可用，应选择最接近的可用方案并如实说明，不能无声跳过配音、字幕包装或 BGM。

## 运行环境路由

开始工作时先从当前运行环境识别操作系统，不要求用户重复说明：

- Windows：可运行跨平台 Python 审查脚本，也可使用 `prepare-scene-review.ps1` 包装脚本；剪映写入仍通过当前可用的桌面控制能力完成。
- macOS：读取 [macos-operation.md](references/macos-operation.md)，使用跨平台 Python 审查脚本；实际控制剪映前确认 Codex/ChatGPT 桌面端的 Computer Use 已启用，并具有“屏幕录制”和“辅助功能”权限。
- 不依赖固定窗口坐标、Windows 专用快捷键、中文界面一定存在或某个按钮永远位于同一位置。应根据当前可见的应用名、版本、语言和控件文字操作；macOS 的撤销键是 `Command` 体系，但优先使用可见按钮和可读状态。
- 不直接改写剪映内部草稿数据库或应用包内容。使用剪映界面导入、保存和导出，并在每个重大阶段读回时间线或预览验证。
- 若当前只有命令行能力，仍可完成事实清单、素材扫描、场景地图、口播和蓝图；只有用户要求实际写入剪映而桌面控制不可用时，才把界面执行标记为待完成。

## 0. 房屋资料与事实层

房产视频开始前，先把用户提供的零散资料整理为事实清单。用户不需要预先写完整口播稿；可接收的资料包括楼盘名称、地区、户型、实用面积、楼层、朝向、空间卖点、景观、交通、周边配套、价格或租金、必须介绍的内容、禁止或不确定的说法。

将信息分成两层，不能混用：

- `provided_fact`：由用户明确提供的楼盘、面积、户型、交通、价格等事实，可用于口播，但不能声称是从画面识别出来的。
- `visible_evidence`：视频中实际可见的空间、设施、景观和动作，只能据此判断镜头内容及出现顺序。

不得从画面猜楼盘、面积、楼层、交通、价格或其他不可见事实，也不得因为资料提到某个空间就假定素材已经拍到。资料缺失时先继续素材扫描和场景地图，把不确定项标记为 `unknown`，不自行补写。只有当缺失事实属于用户要求必须说、且无法安全省略时才请求补充。

用户给出完整稿时，把它视为事实与表达偏好来源，而不是固定时间线：默认允许按真实画面重新排序、拆句和精简；用户标记为“必须原句保留”的句子保持原文，只调整其出现时间。

## 1. 素材扫描与场景地图

已有素材时直接只读扫描，不重复询问常规确认。先用 `ffprobe` 取得分辨率、帧率、时长、旋转信息和音频流，再运行跨平台脚本：

```bash
python3 ./scripts/prepare-scene-review.py --video <源视频> --output <项目/scene-review>
```

Windows 也可使用兼容包装脚本：

```powershell
./scripts/prepare-scene-review.ps1 -VideoPath <源视频> -OutputDir <项目/scene-review>
```

逐张查看固定间隔帧和场景变化帧；长镜头、连续走拍和房产导览应加密抽帧，必要时回看原视频。不得根据文件名、旁白文本或单张画面猜场景。

建立 `scene-map.csv`，每一行必须包含：

```text
source_path,source_in,source_out,scene,subscene,visible_evidence,
camera_motion,shot_quality,start_state,end_state,confidence,notes
```

对房产素材使用 [scene-review-and-blueprint.md](references/scene-review-and-blueprint.md) 的空间分类；无法确定“次卧/主卧”时先标 `bedroom_unknown`，只有画面顺序、空间大小、套间/露台等证据支持时才细分。出现露台必须区分“室内望向露台”“人在露台”“海景特写”。

## 2. 先做蓝图，再碰剪映时间线

完整看片并锁定场景地图之前，不生成最终口播。先根据真实走访顺序、各空间的有效画面长度和事实清单重写口播，再把脚本切成一句只表达一个信息点的 narration beat。每个 beat 必须匹配能直接证明或清楚说明该句的源区间；画面与句子不一致时，优先换镜头、调整口播顺序或精简文案，不能用无关画面硬盖。

读取 [scene-review-and-blueprint.md](references/scene-review-and-blueprint.md)，生成 `edit-blueprint.json`、简明的 `edit-blueprint.md` 和逐句对齐表。每条对齐记录必须包含事实信息、口播句子、对应场景、画面起止时间、口播起点、字幕起点和置信度；每个画面片段必须记录源入点、源出点、时间线入点、用途、对应旁白、速度和置信度。

节奏规则：

- 删除镜头起落、对焦寻找、长时间拍墙/门框、重复扫拍和没有新信息的走空。
- 不把完整源视频作为一个长片段统一变速。必须按场景和有效动作拆成多个源区间。
- 空间揭示、设施细节通常保留约 1.5–4 秒；海景、采光等卖点可更长；走廊和转场通常 0.5–1.5 秒。
- 只对无信息的移动段做 1.5–4 倍加速或抽帧式压缩；房间主体、文字信息和需要观看细节的镜头保持自然速度。
- 相邻镜头避免重复同一构图；切点优先落在开门、转身、移步、推拉摇镜到达主体等动作节点。
- 旁白段落顺序应跟随实际空间出现顺序；交通等无对应室内画面的信息，可放在最稳定的景观/外景段或使用克制文字，不伪装成室内证据。

房产导览还必须遵守以下连续性与覆盖规则：

- 扫描时单独识别玄关、厨房、餐厅/餐区、客厅、露台、海景、次卧、卫生间、主卧等实际出现的空间。`dining_area` 不得并入客厅后遗漏；只要画面证据清楚，就应进入场景地图，并在口播或字幕中获得对应的信息点。
- 叙事顺序以镜头真实走访顺序为准，不套用固定的“卫生间在卧室前”模板。若素材先到次卧、随后才有更长更完整的卫生间镜头，就先介绍次卧，再在卫生间画面开始后介绍卫生间。
- 具名空间的口播不得早于镜头真正进入或清楚揭示该空间。画面仍在门外、走廊或前一个房间时，不得提前出现“主卧”“卫生间”等口播和字幕；宁可延后口播起点、保留短暂自然环境声或适度放慢配音，也不要让字幕抢跑。
- 连续走拍优先保持移动方向和空间关系。可删除明显失焦、遮挡和重复区间，但不得为了迁就口播时长而把进房动作剪得支离破碎；需要压缩时优先缩短无信息等待段，并把切点放在自然动作节点。
- 每个主要空间按“进入/揭示 → 主体展示 → 细节或卖点”分配时长。素材较长且信息更多的空间应获得相应展示时间，不能平均分配或被一句口播仓促带过。
- 资料中存在但视频没有对应画面的卖点，不能使用无关室内镜头伪装成证据。交通信息可以使用用户提供的道路/交通素材；其他缺失画面只使用克制文字或省略，除非用户明确提供了可用补充素材。
- 画面轨必须覆盖到最后一句口播与字幕结束，不能以末帧冻结、黑屏或音乐尾巴补时长。交通段需要加长时可回放另一段道路/交通有效镜头；房屋总结段可回访厨房、客厅、餐区、露台、卧室等对应卖点，但避免紧邻重复同一构图。

## 3. 在剪映中执行并读回检查

读取 [jianying-build-and-audit.md](references/jianying-build-and-audit.md)。在 macOS 上还必须读取 [macos-operation.md](references/macos-operation.md)。使用剪映界面搭建蓝图中的多个独立片段，保持 9:16 源画幅，不拉伸。

声音优先级：配音/人物信息 > 必要环境声 > BGM。用户要求去原声时静音原声；否则只保留有价值的环境声。配音生成后，逐句复核它是否落在对应场景内；需要时调整画面切点，不让“厨房”口播配客厅、“露台”口播配卫生间。

配音锁定后关闭 BGM 干扰，使用剪映“智能字幕/识别字幕”生成可编辑字幕。先快速修正项目名、地名、数字、单位和断句，再进入“文本 → 智能包装”生成与内容和画面风格相符的字幕包装。包装完成后检查字体、颜色、动画、位置、断句和安全区；房产视频优先简洁、高级、易读的方案，不使用遮挡户型细节、跳动过强或喧宾夺主的包装。智能包装生成失败或效果明显不合适时可以重新生成一次，仍不合适则保留统一白字黑边字幕，不能为了包装牺牲可读性。不能用提前写好的整段文本覆盖成一条假字幕。

智能包装完成后必须检查时间线是否同时保留了包装前的基础字幕轨。若包装已生成新的主字幕/强调文字，就隐藏或删除旧基础字幕层，并抽查至少三个同屏时刻，确认没有把同一句话重复显示两遍。智能包装自身设计中的“主字幕＋关键词强调”可以保留，但不能再叠加一行内容相同的旧字幕。

## 4. 剪映音乐库与不重复选曲

本技能默认直接使用剪映内置音乐库，而不是先交付无 BGM 版本。读取 [music-rotation.md](references/music-rotation.md)：

1. 先查看近期选曲记录：

   ```bash
   python3 ./scripts/music-history.py recent --limit 30
   ```

2. 在剪映音乐库搜索与主题匹配的纯音乐/轻音乐/氛围音乐，排除明显人声、强鼓点、版权状态异常或近期已经使用的曲目。
3. 同等适配时优先使用从未用过或最久未使用的曲目；不同视频尽量不重复。
4. 插入后确认音频轨有波形、覆盖完整成片并在结尾淡出。旁白段 BGM 必须明显退位；实际试听，不以音量数值代替听感。
5. 成片验证通过后记录剪映界面显示的准确曲名：

   ```bash
   python3 ./scripts/music-history.py record --track "<曲名>" --project "<项目名>" --source "剪映音乐库"
   ```

若剪映曲库不可访问、未登录、曲目不可用或会员限制，先尝试另一首可用纯音乐；仍失败时才使用用户合法本地音乐，并明确报告降级。不得无声跳过 BGM。

## 5. 审片与交付

完整播放最终导出文件，至少检查：

- 口播中的每个空间/产品/动作是否由当时画面支持。
- 餐厅/餐区等实际拍到的主要空间是否被场景地图、口播和字幕遗漏。
- “主卧”“卫生间”等具名字幕是否等到画面真正进入该空间后才出现；段落顺序是否跟随实际走访顺序。
- 是否仍有无意义的长走拍、墙面、门框、重复扫拍或统一慢放。
- 开头 3 秒是否快速进入主题；镜头长短是否随信息密度变化。
- 智能字幕是否与声音同步，专名、数字和单位是否正确。
- 智能包装是否已经应用且风格统一，字幕是否仍清楚、不过度遮挡主体。
- 智能包装前的旧基础字幕轨是否已关闭，成片中是否不存在重复字幕。
- BGM 是否实际存在、全程不断裂、没有盖过配音、结尾自然淡出。
- 最后一句口播结束前是否一直有运动或有效画面，结尾是否不存在冻结、黑屏或无意义停住。
- 文件是否为目标画幅、帧率，且同时包含视频流和音频流。

发现结构问题必须回到蓝图或时间线修复后重新导出，不能只验证文件可播放。交付成片和可编辑剪映草稿备份，并如实说明仍存在的素材限制。

## 硬阻塞

只在素材不可读、`ffmpeg/ffprobe` 缺失且无法定位、剪映无法保存/导出、磁盘不足，或必须由账号持有人完成登录/付费时暂停。macOS 实际写入还要求 Computer Use 可用并获得“屏幕录制”和“辅助功能”权限；权限缺失时先完成不依赖界面的事实整理、扫描、口播与蓝图，再明确报告界面执行阻塞。普通画面不完美、场景名称低置信度、某首音乐不可用都不是硬阻塞。
